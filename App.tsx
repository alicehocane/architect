import React from 'react';

// DEPRECATED FILE
// This application has migrated to Next.js App Router (SSR).
// The entry point is now 'app/layout.tsx' and 'app/page.tsx'.
// You can safely delete this file and the 'pages/' directory.

const App: React.FC = () => {
  return (
    <div style={{ padding: 50, textAlign: 'center', fontFamily: 'sans-serif' }}>
      <h1>Migration Complete</h1>
      <p>This application now runs on Next.js App Router.</p>
      <p>Please delete <code>App.tsx</code>, <code>index.tsx</code>, and the <code>pages/</code> folder.</p>
    </div>
  );
};

export default App;